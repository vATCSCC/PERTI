# Root .htaccess (may contain server-specific config)
/.htaccess

# .env
.env

# API .htaccess files ARE tracked (needed for Azure Linux deployment)
!api/.htaccess
!api/**/.htaccess

load/config.php

# Bundled tools (download fresh instead)
composer

# Log and lock files
*.log
*.lock
scripts/*.log
scripts/*.lock

# Form data (contains sensitive information)
scripts/ntml_form.json

# Swap/temp files
*.swp
*.swo
*~

# CDM sector GeoJSON (imported into DB, not needed in repo)
assets/geojson/cdm_*.json

# Navigation data CSV imports (regenerate from Navigraph)
adl/php/nav_import/

# Claude Code local settings
.claude/settings.local.json
.claude/credentials.md

# Node.js dependencies (all locations)
node_modules/

# Build/deploy artifacts
*.zip
deploy.zip

# IDE and OS files
.idea/

*.DS_Store
Thumbs.db

# Reference PDFs (FAA documentation)
docs/*.pdf

# TMI Compliance runtime data (saved via web UI, not from repo)
data/tmi_compliance/*.json
data/tmi_compliance/*.html

# Generated indexes (auto-generated every 12 hours)
data/indexes/

# Python build artifacts
__pycache__/
*.egg-info/
*.pyc
*.pyo

# .NET/C# build artifacts
bin/
obj/
*.nupkg
*.snupkg

# Azure configuration
load/azure_config.json
load/azure_perti_config.json
# Git worktrees (isolated workspaces)
.worktrees/
nul
.claude/settings.local.json
brightline_sno_flights.json
2026-01-30-northeast-corridor-fno.json

# Playbook backup exports (auto-generated)
backups/playbook/*.json
backups/playbook/*.txt

# Funding/grant documents (private)
docs/FUND*.md
docs/FUNDING_PACKET*.md

# Playwright MCP artifacts
.playwright-mcp/

# NASR navdata cache (downloaded FAA zip/extractions)
.nasr_cache/

# Navdata ephemeral change reports (generated per-run, not canonical)
assets/data/logs/navdata_changes_*.json
assets/data/logs/navdata_changes_*.txt

# Navdata CSV backups (auto-generated during AIRAC updates, git history suffices)
assets/data/backups/

# Playbook route backups (auto-generated during AIRAC updates)
assets/data/playbook_routes_backup_*.csv

# Root-level PDFs (reference documents, not code)
/*.pdf

# Bash crash dumps
bash.exe.stackdump

# Temporary screenshots
*.png
# But keep tracked assets
!assets/**/*.png

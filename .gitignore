# Root .htaccess (may contain server-specific config)
/.htaccess

# .env
.env

# API .htaccess files ARE tracked (needed for Azure Linux deployment)
!api/.htaccess
!api/**/.htaccess

load/config.php

# Bundled tools (download fresh instead)
composer

# Log and lock files
*.log
*.lock
scripts/*.log
scripts/*.lock

# Form data (contains sensitive information)
scripts/ntml_form.json

# Swap/temp files
*.swp
*.swo
*~

# Navigation data CSV imports (regenerate from Navigraph)
adl/php/nav_import/

# Claude Code local settings
.claude/settings.local.json
.claude/credentials.md

# Node.js dependencies (all locations)
node_modules/

# Build/deploy artifacts
*.zip
deploy.zip

# IDE and OS files
.idea/

*.DS_Store
Thumbs.db

# Reference PDFs (FAA documentation)
docs/*.pdf

# TMI Compliance runtime data (saved via web UI, not from repo)
data/tmi_compliance/*.json
data/tmi_compliance/*.html

# Generated indexes (auto-generated every 12 hours)
data/indexes/

# Python build artifacts
__pycache__/
*.egg-info/
*.pyc
*.pyo

# .NET/C# build artifacts
bin/
obj/
*.nupkg
*.snupkg

# Azure configuration
load/azure_config.json
load/azure_perti_config.json
# Git worktrees (isolated workspaces)
.worktrees/
nul
.claude/settings.local.json
brightline_sno_flights.json
2026-01-30-northeast-corridor-fno.json

# Funding/grant documents (private)
docs/FUND*.md
docs/FUNDING_PACKET*.md

# Playwright MCP artifacts
.playwright-mcp/

# Temporary screenshots
*.png
# But keep tracked assets
!assets/**/*.png
